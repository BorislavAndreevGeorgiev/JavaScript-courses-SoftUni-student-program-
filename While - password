function password(input){

    let userName = input[0];
    let userPass = input[1];

    let index = 3 ;
    let data = input[2];
    index++;


        while(data !== userPass){
            data = input[index];
            index++
        }
    console.log(`Welcome ${userName}!`);
}
